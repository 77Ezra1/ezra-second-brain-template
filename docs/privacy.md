# Privacy guide

This project is designed for local-first personal knowledge management. Treat captured data as private by default.

Do not publish raw messages, inbox/wiki/daily/reviews data, expense records, external service IDs, tokens, cookies, credentials, or private article archives if licensing is unclear.

Use `config/brain.example.yaml` for public examples and keep real `config/brain.yaml` out of git.
